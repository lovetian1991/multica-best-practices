---
name: multica-requirement-analysis
description: 把 Issue / 诉求结构化为带编号的 PRD 内容。用于 @产品经理 需求澄清、范围确认、验收标准定义；结构就绪后交 multica-artifact-req-sync 落地。
---

# 需求分析

## 用途

把 Issue、会议结论、零散想法变成**清晰、可评审、可拆任务**的 PRD 内容，并写入产品设计小队唯一正式交付物 `prd-<功能名称>.md`（只管「写什么」，不管「落到哪个平台」）。

> 本 skill 与 `multica-artifact-req-sync` 分工：**analysis 产出结构与编号；req-sync 负责通过 OpenContent 落地文件**。Issue 数据由 Multica 自身保存。

## 流程

1. 识别业务目标（G- + KPI-）。
2. 识别预期行为与用户角色 / 权限。
3. 识别明确的范围（含 / 不含）。
4. 识别非目标。
5. 识别约束（兼容性 / 性能 / 安全 / 时间）。
6. 识别验收标准（AC-，可测试）。
7. 识别歧义 → 写入 OP- 待确认清单，并标注“阻塞项 / 跟进项”；不装作已确认。
8. 识别依赖与风险（RISK-）。

## 需求结构

把输入结构化为 `prd-<功能名称>.md` 章节，并叠加 Multica 编号。`<功能名称>` 必须取自本次实现的功能，使用简短、稳定、可读的中文名称，例如 `用户邀请`、`订单退款`；禁止使用笼统的 `prd.md`，同一功能后续变更更新原文件。

| 章节 | 编号 |
| --- | --- |
| 一句话定义、背景 | — |
| 目标与成功标准 | G- + KPI- |
| 用户故事 | U- |
| 功能需求 | FR- |
| 业务规则 | BR- |
| 验收标准 | AC- |
| 待确认项 | OP- |
| 风险项 | RISK- |

## 固定功能清单

详细功能清单必须使用以下字段，不能只写散文式描述：

`编号 | 模块 | 功能名称 | 用户/角色 | 触发条件 | 输入 | 处理逻辑 | 输出 | 优先级 | 依赖 | 对应 AC-`

每个功能都必须能追溯到至少一个 FR-、BR-（如适用）和 AC-。需求设计还必须明确前置条件、主流程、分支流程、异常流程和边界条件。

## 单文档协作规则

- 产品经理 先创建或更新本次功能对应的 `prd-<功能名称>.md`，完成产品定义、功能清单、需求设计、业务规则和 AC-。
- UI/UE设计师 仅在 产品总监 派单后，顺序补写同一份功能 PRD 文件的 UI / UX 章节。
- 技术架构师 在产品经理完成后按需补写同一份功能 PRD 文件的技术约束 / 可行性章节；只有 UI/UE设计师 与技术架构师同时启用时，才需等待 UI/UE设计师 完成并释放文档。
- UI/UE设计师 与 技术架构师 不得并发修改同一份功能 PRD 文件；独立 UI / 技术文档只能作为引用或工作草稿，不能替代功能 PRD 文件。
- 产品总监 最终只评审同一份功能 PRD 文件，不要求额外的独立最终文档。

正式 PRD 至少包含（详见 @产品经理 角色指令）：一句话定义、背景、目标 G- + KPI-、用户与权限、范围、FR-/BR-/AC-、字段口径、空态 / 异常态 / 无权限态、RISK-/OP-、修订记录。

## 输出

正式执行时必须创建或更新本次功能对应的 `prd-<功能名称>.md`，不要只在回复中输出一段临时分析。

- **目标（G-）**：要解决什么问题
- **范围**：要改什么
- **非目标**：明确不改什么
- **验收标准（AC-）**：可测试的检查项
- **约束**：兼容性 / 性能 / 安全 / 时间
- **依赖**：前置条件
- **待确认项（OP-）**：集中维护；只有影响当前范围、业务规则、权限 / 数据口径、AC- 或安全合规的阻塞项未关闭时，才不得进入受影响的设计 / 开发
- **风险（RISK-）**：需关注的风险

## 规则

不要默默消化有歧义的需求。

如果歧义会实质影响当前范围、业务规则、权限 / 数据口径、验收或安全合规：

→ BLOCKED  
→ 说明缺什么、谁提供，只问 1 个最关键问题。

如果只是实现偏好、文案细节、可后补资料或不影响当前 AC- 的跟进事项：

→ 标记为非阻塞跟进项
→ 记录负责人和后续时间，不阻塞当前产品定义。

## 交接

结构就绪后，用 `multica-artifact-req-sync` skill 将同一份功能 PRD 文件上传/更新到 OpenContent，写入 Multica Issue metadata/comment，并回传稳定 internal_link。

> @产品经理：「先用 `multica-requirement-analysis` 结构化 PRD，再用 `multica-artifact-req-sync` 落地并回传链接。」

## 为什么有效

需求阶段的歧义会在后续每个阶段被放大。用 BLOCKED 挡住歧义、用编号让下游可拆任务；内容与平台解耦后，只替换 artifact-sync 平台实现，不动分析逻辑。
